/target
test_ag_home6/data/vectors.new.json
test_ag_home6/data/vectors.new.json
.fastembed_cache/
tmp/

# Large model weights — re-downloadable artifacts, not versioned
models/ner/model.onnx
models/layout_detr/model.onnx
models/embedding_model.onnx
models/embedding_model.onnx.data
backend/models/embedding_model.onnx
.aider*
.env
.env
.env
.env
.env
.env
.env
.env
.env
.env
.env
.env
.env
.env.graph
.claude/

# Editor / backup junk
*.bak*
*.swp

# Installer release artifacts — published via GitHub Releases, not Git
*.AppImage
*.AppImage.sha256

# FalkorDB binaries extracted from the pinned docker image (~75 MB).
# CI's release.yml extracts these on each build via docker create + cp,
# and build-appimage.sh reads from this path; never commit them.
installer/stage/
