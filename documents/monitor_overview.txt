# Monitor Overview Snapshot (Updated)

## Requests Monitoring
- Pages: `/monitor/requests/*`
- Components: `SummaryTile`, `LatencyGraph`, `PercentileGrid`
- Endpoints: `/monitor/requests/throughput`, `/monitor/requests/latency`, `/monitor/requests/errors`, `/monitor/requests/active`

## System Health
- Covers Tantivy Index, SQLite, Redis, Rate Limiter
- Uptime tile uses `/monitor/system/uptime`
- Component cards: `CompTile`

## Index & Storage
- Endpoint `/monitor/index/size`
- Reports total size, per-store usage, disk remaining

## Rate Limits
- Configuration page (`Monitor > Rate Limits > Configuration`)
- Shows global env toggles (RATE_LIMIT_ENABLED, TRUST_PROXY)
- Per-route limits table with QPS + Burst

## Tooling (Task 19 additions)
- **Tools Page** now renders:
  - `/monitoring/tools/cache` # cache hit-rate + entries per tool
  - `/monitoring/tools/rate-limits` # token buckets per tool (utilization, window)
  - `/monitoring/tools/costs` # execution cost ledger per tool (total, avg, last updated)
  - `/monitoring/tools/dependencies` # tool-chain edges/nodes recorded by executor
- Usage distribution pulled from `/monitoring/tools/stats`
- UI sections:
  - Cache Layers panel with aggregate cards + table (enabled caches, entries, avg hit rate)
  - Rate Limiters panel with utilization bars & status tags
  - Tool Costs panel summarizing total cost, exec count, top spender + table
  - Dependency graph cards plus lists for busiest nodes/edges

## RAG Monitor
- `/monitoring/memories/rag?limit=50` and `/monitoring/chunking/latest`
- Includes detection observability table, memory cards

## Hardware + Home Tiles
- Home uses summary of total docs, model/hardware info, etc.

## Troubleshooting Notes
- Dioxus runtime errors (hooks ordering, async tasks) noted in console log
- When the frontend router context is missing, ensure `NavTabs` usage stays within router scope
- Hydration warnings appear if `dx serve --platform web` hot reload injects placeholders

## Next Steps
- Add sparkline visualization for Tool Costs (per-tool trend)
- Consider mini graph for dependency edges (force-directed) once Dioxus canvas util ready
